# Date:27-Jan-2026

1. Create a Calculator caomponent taht looks like the Windeox Calculator
    - No Google, copilot
2. Modify the TableGridComponent as follows:
    - Generate a Delete Button, when this button ius clicked the record MUST be deleted from the Parent's collection of which value are causing the TableGrid to generate rows    
    - For each row generate CheckBox for Multi-Selecting Records, the parent component will decide wheather the checkboxes are rendered or not
    - Each Column Header will have the Sort and Reverse buttons, to sort and reverse table conmtents. The Parent component will decide if the Tab;egrid to be sorted or reversed
Date on Submission is on Thursday 29-Jan-2026 


# Date: 04-Feb
- Update and Delete using TabStack Query
